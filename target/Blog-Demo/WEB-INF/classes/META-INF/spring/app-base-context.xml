<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:jpa="http://www.springframework.org/schema/data/jpa"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-4.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa-1.8.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.2.xsd">

	<!-- To use transactions. -->
	<tx:annotation-driven transaction-manager="transactionManager" />

	<!-- ***********Define the base package of repository ***************************** -->
	<jpa:repositories base-package="com.monika.jba.repository"></jpa:repositories>

	<!-- TODO: Later will use some production database e.g. mySQL or Oracle -->
	<!-- in memory database. -->
	<jdbc:embedded-database type="HSQL" id="dataSource" />
	
	<!-- Will implement while adding JNDI
	<jee:jndi-lookup jndi-name="java:comp/env/jdbc/dbName" resource-ref="true" 
		lookup-on-startup="true"/> TODO: will get from properties file. -->

	<bean
		class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean"
		id="entityManagerFactory">
		<property name="packagesToScan" value="com.monika.jba.entity" />
		<property name="dataSource" ref="dataSource" />
		<property name="jpaProperties">
			<props>
				<!-- TODO: Will change this.-->
				<prop key="hibernate.show_sql">true</prop>
				<!-- TODO: Will change this. -->
				<prop key="hibernate.hbm2ddl.auto">create</prop>
				<prop key="hibernate.enable_lazy_load_no_trans">true</prop>
			</props>
		</property>
		<property name="persistenceProvider">
			<bean class="org.hibernate.jpa.HibernatePersistenceProvider" />
		</property>
	</bean>

	<bean class="org.springframework.orm.jpa.JpaTransactionManager"
		id="transactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>

	<!-- Schedule saving the item in database -->
	<!-- <task:annotation-driven/> -->


</beans>
